# Checkpoint rules

The reading record permits at most three checkpoints, with unique slots: `commit`, `work`, and `reconcile`. Published chapters require all three in that order. Each checkpoint needs an existing passage anchor, a stable excerpt hash, a supported strategy, and a clear response structure.

Use `list_passages` before drafting. Validate before submission; do not approve or publish without explicit authorization.
